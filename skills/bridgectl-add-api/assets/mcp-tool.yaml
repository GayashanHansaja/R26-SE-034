apiVersion: erpbridge.io/v1
kind: MCPTool
metadata:
  name: <NAME>
  module: <MODULE>
spec:
  description:
    short: ""         # ← fill in
    whenToUse: []      # ← fill in
    whenNotToUse: []   # ← fill in when similar tools exist
    examples: []       # ← fill in
  execution:
    type: http
    method: <METHOD>
    endpoint: <PATH>
    responsePath: data
  security:
    authType: api-key
    credentialRef: ERP_PRIMARY_KEY
