export function useChatSessions() {
  return {
    sessions: [
      { id: "chat-1", title: "Invoice exception resolver" },
      { id: "chat-2", title: "Inventory reorder planner" },
    ],
  };
}

export default useChatSessions;
