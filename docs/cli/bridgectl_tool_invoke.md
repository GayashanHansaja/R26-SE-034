## bridgectl tool invoke

Invoke an MCP tool directly

### Synopsis

Perform a direct invocation of an MCP tool through the middleware's 
internal API. This bypasses the full MCP transport but follows the same 
logic (including caching and validation), making it ideal for testing 
how an AI agent would experience the tool.

```
bridgectl tool invoke [name] [arguments] [flags]
```

### Examples

```
  bridgectl tool invoke finance.get-invoices '{"page": 1}'
```

### Options

```
  -h, --help   help for invoke
```

### Options inherited from parent commands

```
  -c, --context string   Override active context
  -o, --output string    Output format: table, json, yaml (default "table")
  -v, --verbose          Show full HTTP request/response detail
```

### SEE ALSO

* [bridgectl tool](bridgectl_tool.md)	 - Manage MCP tool schemas

###### Auto generated by spf13/cobra on 6-May-2026
