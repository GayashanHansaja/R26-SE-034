## bridgectl tool validate

Validate an MCP tool schema (JSON) or an OpenAPI spec (YAML)

### Synopsis

Pre-flight check for schema files. This command validates that 
a JSON schema follows the MCP tool structure or that an OpenAPI 
specification can be correctly parsed and transformed by ERPBridge.

```
bridgectl tool validate [file] [flags]
```

### Examples

```
  bridgectl tool validate schemas/finance/get-invoices.json
  bridgectl tool validate ./specs/legacy-erp.yaml
```

### Options

```
  -h, --help   help for validate
```

### Options inherited from parent commands

```
  -c, --context string   Override active context
  -o, --output string    Output format: table, json, yaml (default "table")
  -v, --verbose          Show full HTTP request/response detail
```

### SEE ALSO

* [bridgectl tool](bridgectl_tool.md)	 - Manage MCP tool schemas

###### Auto generated by spf13/cobra on 6-May-2026
